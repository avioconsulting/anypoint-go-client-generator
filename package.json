{
  "name": "cloudhub-automation-client-generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate": "export ANYPOINT_GENERATOR_GO_DEST=`pwd`/dist && npx openapi-generator-cli generate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@souf",
  "license": "ISC",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.4.18"
  }
}
